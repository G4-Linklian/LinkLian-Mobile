{
  "name": "linklian-mobile-ci",
  "private": true,
  "version": "1.0.0",
  "description": "Flutter CI commands (npm, no .sh, no sonar)",
  "scripts": {
    "ci:deps": "flutter pub get",
    "ci:format": "dart format --set-exit-if-changed lib test",
    "ci:analyze": "flutter analyze",
    "ci:lint": "flutter analyze",
    "ci:test": "flutter test --coverage",
    "ci:sca": "flutter pub outdated || true && flutter pub deps || true && (flutter pub audit || true)",
    "ci:all": "npm run ci:deps && npm run ci:format && npm run ci:analyze && npm run ci:test && npm run ci:sca"
  }
}

